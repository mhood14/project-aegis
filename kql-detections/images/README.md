# Images

This folder is reserved for future detection screenshots, workbook exports, and alert evidence specific to the KQL detections package.

Current evidence images already exist in `SupportingDocs/AppPictures/` at the repo root.
